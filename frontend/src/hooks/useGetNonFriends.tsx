import React, { useEffect } from "react";

const useGetNonFriend = () => {
    const [loading, isLoading] = React.useState<boolean>(false);
    const [nonFriends, setNonFriends] = React.useState<ConversationType | null>();

    useEffect(() => {

    }, []);

    
}